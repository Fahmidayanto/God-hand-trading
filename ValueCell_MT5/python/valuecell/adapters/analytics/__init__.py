"""Analytics subpackage — offline tooling built on top of ``DecisionLogger``.

Ponytail choices:
- One subpackage, currently two thin stdlib-only scripts (``replayer``,
  ``daily_report``). Anything reusable from ``scripts/`` that grows legs
  should graduate in here.
"""
