# Workspace Active Skills & Rules - Project MT5

> Instruksi aktif untuk workspace lokal `B:\Project MT5`.
> Repository: `God-hand-trading` (MT5 Automated Trading System & Market Structure Platform).
## INSTRUKSI TIDAK BOLEH DILANGGAR !!!!!

contoh percakapan agent jawab sendiri percakapan !!!!! :
"Codebase Memory tersedia. Saya cek apakah workspace sudah memiliki konfigurasi/integrasi Headroom agar panduan tidak menduplikasi setup yang ada.
&#x20; Search code
Project `AI-video` belum terindeks di Codebase Memory, jadi kanal itu tidak dapat dipakai tanpa membuat indeks baru. Saya lanjut memakai repo dan dokumentasi resmi Headroom; tidak akan mengindeks atau mengubah workspace."

(TIDAK PERLU PERCAKAPAN INTERNAL !!!!!!!, CUKUP BERIKAN HASIL INFORMASI!!!!!)

## Instruksi Dasar (Wajib Turn 1)

CRITICAL BLOCKING GATE:
Pada pesan PERTAMA di setiap sesi baru, SEBELUM menghasilkan teks respon apa pun (termasuk menjawab pertanyaan `?`, menganalisis error log, atau investigasi), AI WAJIB mengeksekusi tool `view_file` secara paralel untuk membaca 3 file skill dasar berikut:

1. **caveman**: `C:\Users\fahmi\.gemini\config\skills\caveman\SKILL.md`
2. **ponytail**: `C:\Users\fahmi\.gemini\config\skills\ponytail\SKILL.md`
3. **i-have-adhd**: `C:\Users\fahmi\.gemini\config\skills\i-have-adhd\SKILL.md`

Dilarang keras mengirimkan respon teks ke pengguna sebelum tool call membaca 3 skill di atas dipanggil pada giliran pertama sesi.

Aturan komunikasi:

1. Selalu gunakan Bahasa Indonesia untuk percakapan, temuan, ringkasan, dan pertanyaan.
2. Gunakan bahasa yang mudah dipahami (*easy & balanced*). Dilarang membuat analogi yang berlebihan atau mengada-ada.
   - **Standar Format Teks & Angka (Strict Plain Text):** DILARANG KERAS menggunakan format KaTeX/LaTeX, escape backslash, rumus math block, atau tanda dollar (`$`, `$$`, `\text{...}`, `\times`, `\ge`, `\le`, `\mathbf`) dalam bentuk apa pun karena merusak kerapian tampilan. Semua rumus matematika, perhitungan finansial, dan angka WAJIB SELALU disajikan dalam format teks biasa (*strict plain text* atau inline code sederhana seperti `Modal Awal + Akumulasi Net_Profit = Saldo Akhir`).
   - **Tabel Padanan Wajib:**
     - Perkalian: wajib huruf `x` (contoh: `3 x ATR(14)`, `27,26 x 10 = 272,6 Pips`)
     - Lebih besar sama dengan: wajib `>=` (contoh: `Profit >= 300 USD`)
     - Lebih kecil sama dengan: wajib `<=` (contoh: `Jarak <= 3000 poin`)
     - Lebih besar / Lebih kecil: wajib `>` atau `<` (contoh: `Jarak > 3000 poin`, `Rasio < 40%`)
     - Penjumlahan / Pengurangan: wajib tanda `+` atau `-` biasa (contoh: `1000 + 3233.97 = 4233.97 USD`)
3. Gunakan gaya ringkas dan presisi sesuai `caveman`.
4. Gunakan solusi paling sederhana yang benar sesuai `ponytail`: utamakan pola repository, fitur native, dan dependency yang sudah terpasang.
5. Terapkan prinsip `i-have-adhd`: tindakan langsung (*action first*), batasi list maksimal 5 item, langkah bernomor, tanpa basa-basi (*zero fluff*), dan estimasi waktu nyata.
6. Jangan mengurangi validasi input, keamanan, aksesibilitas, penanganan error, atau perlindungan data demi menghemat kode.
7. **Standar Diagram & Alur:** DILARANG KERAS menggunakan format kode Mermaid (karena membingungkan/sulit dibaca). Wajib menyajikan diagram atau alur sistem dalam format visual terstruktur, rapi, dan mudah dibaca (seperti ASCII/box card layout yang presisi atau tabel breakdown komprehensif) tanpa teks mentah yang membingungkan.
8. **Standar Pelaporan PnL & Metrik Finansial (Strict Net Profit Rule):**
   - Dalam setiap analisis, perbandingan, pembacaan CSV, dan pelaporan metrik trading (baik EA MT5 maupun Replay Trades), AI WAJIB SELALU menggunakan **Kolom `Net_Profit` (Keuntungan Bersih)** yang sudah memperhitungkan biaya spread, komisi, dan swap. DILARANG KERAS menggunakan kolom `Profit` (Gross) sebagai representasi Net PnL.
   - **Standar Net PnL / Total Balance Workspace:** Di frontend dashboard dan laporan performa, "Net PnL" / "Account Value" merepresentasikan Total Saldo Akhir (`Modal Awal Deposit + Akumulasi Net Profit`).

## Persetujuan Sebelum Eksekusi

Jangan otomatis menjalankan command, script, pipeline, instalasi, migrasi, deployment, mutasi database, atau perubahan file.

Sebelum tindakan tersebut:

1. Jelaskan tindakan dan dampaknya secara singkat.
2. Minta konfirmasi pengguna.
3. Tunggu persetujuan eksplisit.
4. Jalankan hanya tindakan yang sudah dijelaskan.

Operasi baca saja seperti membaca file, mencari simbol, melihat status Git, dan query Codebase Memory boleh dilakukan untuk investigasi awal. Jangan meminta secret, token, password, atau API key melalui chat; minta pengguna memasukkannya langsung melalui mekanisme aman.

Jika pesan pengguna mengandung tanda tanya (`?`), jawab pertanyaannya terlebih dahulu secara informatif dan transparan. Jangan melakukan mutasi atau eksekusi sebelum ada penjelasan dan persetujuan.

## Alur Dev, Bugfix, dan Refactor

Untuk bugfix, fitur, enhancement, atau refactor:

1. Investigasi akar masalah atau jalur implementasi terlebih dahulu.
2. Jelaskan temuan, hipotesis, dampak, dan validasi termurah yang relevan.
3. Rekomendasikan 2-3 skill dengan format icon murni `❌ [nama-skill]` saat memulai task/domain baru (format ini adalah transparansi rencana internal AI, bukan instruksi agar pengguna mengetik slash command).
4. Minta konfirmasi sebelum perubahan file atau eksekusi command.
5. Setelah disetujui, buat perubahan minimum pada owning module.
6. Jalankan validasi terfokus setelah perubahan, lalu validasi lebih luas sesuai risiko.
7. Ubah status skill yang benar-benar dipakai menjadi `✅ [nama-skill]`.
8. Tawarkan pencatatan ke Obsidian Vault (`B:\SecBrain\TradingBrain`) hanya jika hasilnya bernilai sebagai dokumentasi project atau knowledge reusable.

Catatan siklus hidup skill:
- **Skill Persistent dalam Sesi:** Skill yang sudah dibaca dan berstatus `✅ [nama-skill]` otomatis tersimpan dalam memori sesi aktif dan TIDAK BOLEH di-reset menjadi `❌` lagi dalam sesi yang sama.
- **Penggunaan Format `❌ [nama-skill]`:** HANYA digunakan untuk skill BARU yang belum pernah dimuat/dibaca dalam sesi berjalan saat memulai task/domain baru.
- **Jika Semua Skill Relevan Sudah Aktif (`✅`):** Cukup cantumkan status aktifnya `✅ [nama-skill]` sebagai konfirmasi kesiapan tanpa membuat saran `❌` baru.
- **Dilarang Mengulang Saran:** Dilarang mengulang saran `❌` untuk skill yang sudah berstatus `✅`.
- **Percakapan Non-Task:** Tidak perlu rekomendasi skill untuk percakapan casual, tanya-jawab penjelasan, navigasi sederhana, atau perbaikan minor lanjutan.

## Project dan Arsitektur

Project MT5 adalah platform automated trading, market structure tracking (HH, LL, CHoCH, BoS), backtest analytics, dan operational dashboard untuk MetaTrader 5 (khusus instrumen Gold / XAUUSD).

```text
Project MT5/
  Other/
    Strategy_all/        Source code Expert Advisor MetaTrader 5 (Dev_Bot_v11_Gold.cs, Dev_Bot_v12_GoldO.cs, dll.)
  Backtest_result/       Output CSV backtest, market structure state (*.txt), market data (M15/H1/H4), session zones
  Analysis_Backtest/     Notebooks & skrip analisis data backtest & evaluasi performa strategi
  ValueCell_MT5/
    backend/             FastAPI / Uvicorn server, sentiment data & market APIs (Python 3.11, port :8000)
    frontend/            React / Vite SPA operational dashboard (port :5173)
    python/              Modul Python pendukung trading logic & live bridge
    venv/                Virtual environment lokal ValueCell_MT5
  python/                Market structure state engine, verification logic & parser
  verification_system/   Test suite & verification runner
  scratch/               Skrip inspeksi, verifikasi sementara & debug
  tools/                 Helper scripts & monitoring tooling
```

Prinsip ownership:

- Logika EA trading & state tracking live/backtest berada di `Other/Strategy_all/` (MQL5/C#).
- File state runtime dan log trading historis tersimpan di `Backtest_result/`.
- Backend API server berada di `ValueCell_MT5/backend/` (FastAPI).
- Dashboard visualisasi web berada di `ValueCell_MT5/frontend/` (React/Vite).
- Parser, kalkulasi market structure python, dan unit verifikasi berada di `python/` dan `verification_system/`.
- Seluruh skrip debug, pemeriksaan database, atau verifikasi sementara WAJIB dibuat di dalam folder `scratch/` atau `Check_bug/`. DILARANG menaruh skrip scratch di root workspace.

## Codebase Memory MCP

Gunakan project Codebase Memory `Project MT5` sebagai engine utama eksplorasi kode. Daemon berjalan lokal dengan UI 3D di port `:9749`.

Urutan prioritas:

1. `search_graph`: cari function, class, route, type, dan variable.
2. `trace_path`: petakan caller, callee, dan data flow.
3. `get_code_snippet`: baca implementasi simbol secara terarah.
4. `query_graph`: analisis dependency atau pola multi-hop.
5. `get_architecture`: lihat topologi tingkat tinggi jika diperlukan.

Sebelum mengubah signature, route contract, shared type, atau utility bersama, jalankan impact analysis inbound. Gunakan text search untuk string literal, config key, log message, Markdown, YAML, Dockerfile, atau saat graph tidak memiliki simbol yang dicari.

Indeks disimpan oleh layanan Codebase Memory di luar repository. Saat reindex gunakan `persistence: false`.

## Skill Matrix & Dynamic Skill Discovery

Seluruh perbendaharaan skill tersimpan di direktori konfigurasi global:
`C:\Users\fahmi\.gemini\config\skills\`

Setiap kali pengguna meminta pengerjaan tugas baru (fitur, bugfix, refactor, perancangan sistem, optimasi visual, atau audit):
1. **Dynamic Skill Search:** Pindai dan temukan skill paling relevan di `C:\Users\fahmi\.gemini\config\skills\` secara dinamis berdasarkan deskripsi `SKILL.md` dan domain tugas yang dihadapi.
2. **Suggest 2-3 Skill ke User:** Tampilkan 2-3 rekomendasi skill terbaik dengan format `❌ [nama-skill]` sebagai deklarasi standar yang akan dipegang AI.
3. **Session Persistence:** Skill yang sudah dimuat dalam sesi aktif tetap berlaku di memori tanpa perlu dipanggil ulang di setiap giliran pesan.
4. **Baca SKILL.md:** Baca file `SKILL.md` dari skill yang dipilih sebelum menulis kode atau menjalankan perubahan jika belum ada di memori sesi.
5. **Update Status Pasca-Eksekusi:** Ubah status skill yang benar-benar diterapkan menjadi `✅ [nama-skill]`.

### Skill Dasar (Selalu Aktif)
- `caveman`: `C:\Users\fahmi\.gemini\config\skills\caveman\SKILL.md` (Komunikasi padat, ringkas, dan presisi Bahasa Indonesia)
- `ponytail`: `C:\Users\fahmi\.gemini\config\skills\ponytail\SKILL.md` (Solusi paling sederhana yang benar: utamakan native dan pola existing)
- `i-have-adhd`: `C:\Users\fahmi\.gemini\config\skills\i-have-adhd\SKILL.md` (Action first, langkah bernomor, batasi list max 5, zero fluff)

### Katalog Domain Relevan (Global Config Skills)
- **Trading & Strategy:** `latency-critical-systems`, `defi-amm-security`, `llm-trading-agent-security`
- **Backend & Python:** `python-patterns`, `python-testing`, `backend-patterns`, `fastapi-patterns`
- **Frontend & Web UI:** `frontend-patterns`, `design-system`, `motion-ui`, `accessibility`
- **Quality & Verification:** `verification-before-completion`, `verification-loop`, `tdd-workflow`, `rtk-tdd`
- **Agent & Harness:** `agent-harness-construction`, `agentic-engineering`, `continuous-learning-v2`, `prompt-optimizer`
- **Architecture & Graph:** `codegraph`, `search-first`, `system-architecture-canvas`, `graphify`

## Command Guide

Semua command dijalankan dari root `B:\Project MT5`, setelah persetujuan pengguna.

```powershell
# Development Launchers
.\start_backend.bat           # Menjalankan FastAPI Uvicorn backend di http://localhost:8000
.\start_frontend.bat          # Menjalankan React Vite frontend dashboard di http://localhost:5173
.\start_mcp.bat               # Menjalankan daemon codebase-memory-mcp di http://localhost:9749
.\start_headroom_proxy.bat    # Menjalankan headroom proxy
.\start_headroom_monitor.bat  # Menjalankan headroom monitor

# Python Backend (Manual / Focused)
cd ValueCell_MT5\backend
..\venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Testing & Quality
.\ValueCell_MT5\venv\Scripts\pytest.exe verification_system/
.\ValueCell_MT5\venv\Scripts\pytest.exe ValueCell_MT5/backend/
```

Gunakan test paling sempit yang membuktikan perubahan terlebih dahulu sebelum menjalankan full test suite.

## Environment dan Infrastruktur

- **Runtime Backend**: Python 3.11 (`ValueCell_MT5\venv` dan root `.venv`).
- **Runtime Frontend**: Node.js & npm (`ValueCell_MT5\frontend`).
- **Trading Engine**: MetaTrader 5 Terminal (EA live trading & strategy tester backtesting).
- **Service Endpoints**:
  - FastAPI Backend API: `http://localhost:8000` (Swagger Docs: `http://localhost:8000/docs`).
  - Frontend Dashboard: `http://localhost:5173`.
  - Codebase Memory Graph UI: `http://localhost:9749`.
- **Environment & Secrets**:
  - `.env.example` adalah kontrak variable; file `.env` bersifat rahasia dan tidak boleh dikomit.
  - Jangan menampilkan secret, password, atau credential dalam log, output, atau chat.

## Git Tracking

- Workspace lokal: `B:\Project MT5`.
- Git repository: `God-hand-trading`.
- Branch utama: `main` / `master`.
- Aturan `.gitignore`:
  - File backtest besar (`Backtest_result/*.csv`, `catboost_info/`).
  - Compiled binaries EA (`*.ex5`).
  - Virtual environments (`.venv/`, `venv/`).
  - Tooling lokal AI (`.agents/`, `.claude/`, `.gemini/`, `CLAUDE.md`).
  - Environment file (`.env`, `*.env`).
- Jangan commit atau push kecuali diminta eksplisit oleh user.
- Jangan mengubah, menghapus, atau me-revert perubahan pengguna yang tidak terkait.

## Obsidian Vault

Vault aktif: `B:\SecBrain\TradingBrain`.

Struktur folder target:

- `Notes/`: Catatan umum, temuan pasar, observation logs.
- `Project/`: Konteks kerja aktif, keputusan strategi, ringkasan backtest, post-mortem.
- `Templates/`: Standard note formats dan template evaluasi backtest.
- `Archive/`: Historical logs dan completed backtest runs.

Setiap note baru harus memiliki YAML frontmatter minimal `title`, `tags`, `type`, `created`, dan `updated`. Gunakan heading terstruktur dan wikilink `[[Nama_Catatan]]` jika ada hubungan nyata. Jangan otomatis membuat note; usulkan path dan nama note, lalu tunggu persetujuan pengguna.

Format usulan pasca-eksekusi:

- Folder: `Notes/` atau `Project/`
- Note: `[[Nama_Catatan]]`
- Status: `[STATUS: MENUNGGU PERSETUJUAN]` atau `[STATUS: SUDAH TEREKSEKUSI]`

## Definition of Done

Pekerjaan hanya boleh dinyatakan selesai jika:

1. Perubahan sesuai permintaan dan scope repository `God-hand-trading`.
2. Tidak ada file temporary / scratch yang diletakkan di root folder.
3. Validasi fresh yang relevan sudah dijalankan setelah persetujuan.
4. Hasil validasi dibaca dan kegagalan dilaporkan apa adanya.
5. Dokumentasi diperbarui dan ditawarkan pencatatan ke Obsidian Vault `B:\SecBrain\TradingBrain`.